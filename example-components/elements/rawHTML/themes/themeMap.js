import * as styles from './styles';
import * as largeStyles from './large';
import * as standardStyles from './standard';
import * as thinStyles from './thin';
import * as timestampStyles from './timestamp';

const themeMap = {
  default: styles,
  large: largeStyles,
  standard: standardStyles,
  thin: thinStyles,
  timestamp: timestampStyles,
};

export default themeMap;
