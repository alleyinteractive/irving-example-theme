import * as alignMiddleStyles from './alignMiddle';
import * as defaultStyles from './default';
import * as fullWidthStyles from './fullWidth';
import * as sclaeFitStyles from './scaleFit';

const themeMap = {
  alignMiddle: alignMiddleStyles,
  businessGrid: defaultStyles,
  default: defaultStyles,
  fullWidth: fullWidthStyles,
  scaleFit: sclaeFitStyles,
};

export default themeMap;
